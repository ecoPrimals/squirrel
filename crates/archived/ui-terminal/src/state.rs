
/// Represents the application state
#[derive(Debug, Default)]
pub struct AppState {
    // Placeholder for state, not actually used in our simplified version
}

impl AppState {
    /// Create a new app state
    pub fn new() -> Self {
        Self::default()
    }
} 