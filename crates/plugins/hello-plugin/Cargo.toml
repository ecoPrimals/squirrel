[package]
name = "hello-plugin"
version = "1.0.0"
edition = "2021"
authors = ["DataScienceBioLab"]
description = "A simple hello world plugin for Squirrel CLI"
license = "AGPL-3.0-only"

[lib]
name = "hello_plugin"
crate-type = ["cdylib"]

[dependencies]
squirrel-commands = { path = "../../services/commands" }
squirrel-cli = { path = "../../tools/cli" }
async-trait = "0.1"
log = "0.4"
clap = { version = "4.4", features = ["derive"] } 