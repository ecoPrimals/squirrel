[package]
name = "universal-patterns"
version.workspace = true
edition.workspace = true
authors.workspace = true
license = "AGPL-3.0-or-later"
description = "Universal transport, IPC, and operational patterns for ecoPrimals"
repository = "https://github.com/ecoPrimals/squirrel"
keywords = ["ecoprimals", "ai", "mcp", "json-rpc"]
categories = ["development-tools"]

[lints]
workspace = true

[dependencies]
tokio.workspace = true
universal-constants = { path = "../universal-constants", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
bytes = { version = "1.5", features = ["serde"] }
dirs.workspace = true

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3.0"
proptest.workspace = true
temp-env = "0.3"
