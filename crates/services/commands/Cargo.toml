[package]
name = "squirrel-commands"
version.workspace = true
edition.workspace = true
license = "AGPL-3.0-only"
authors.workspace = true



[dependencies]
# Workspace crates
squirrel-interfaces.workspace = true
squirrel-context.workspace = true
squirrel-core = { path = "../../core/core", version = "0.1.0" }
# squirrel-app.workspace = true  # → Eliminated: Songbird handles service layer
# squirrel-ai-tools.workspace = true  # → Eliminated: Moved to ToadStool

# External dependencies
async-trait.workspace = true
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
thiserror.workspace = true
uuid.workspace = true
chrono.workspace = true
clap.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
async-recursion.workspace = true
futures.workspace = true
glob.workspace = true
tempfile.workspace = true
parking_lot.workspace = true
# once_cell removed - evolved to std::sync::OnceLock/LazyLock (stable Rust 1.80+)
bytes.workspace = true
dashmap.workspace = true
regex.workspace = true
universal-constants = { path = "../../universal-constants", version = "0.1.0" }
secrecy.workspace = true

# gRPC REMOVED - NOT ecoPrimals standard!
# ecoPrimals uses JSON-RPC + tarpc (full Rust ecosystem)
# tonic REMOVED
# prost REMOVED

# Remove orchestrator dependency - moved to Songbird project
# nestgate-orchestrator = { path = "../nestgate-orchestrator", version = "0.1.0" }

[dev-dependencies]
tokio-test.workspace = true
pretty_assertions.workspace = true
uuid.workspace = true
clap.workspace = true

[features]
default = []
# ecoBin: system-metrics uses universal_constants::sys_info (pure Rust, /proc on Linux)
system-metrics = []

[lints]
workspace = true
